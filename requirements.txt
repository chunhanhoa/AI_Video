# Core Python packages
setuptools>=65.5.0
wheel>=0.38.0
numpy>=1.26.0

# Image and video processing
opencv-python-headless>=4.7.0  # Headless version avoids GUI dependencies
pillow>=9.0.0

# AI and ML frameworks
onnx>=1.14.0
insightface>=0.7.3  # Specify exact version that has FaceSwap
tensorflow>=2.12.0
gfpgan>=1.3.8

# UI and utility packages
streamlit>=1.22.0
psutil>=5.9.0
tqdm>=4.60.0
protobuf>=4.23.0
opennsfw2>=0.10.0

# Runtime dependencies
onnxruntime>=1.15.0; sys_platform == 'darwin'
onnxruntime-gpu>=1.15.0; sys_platform != 'darwin'

# Additional dependencies
facexlib>=0.3.0  # Optional dependency for face enhancement
moviepy>=1.0.3  # Better video handling than OpenCV
